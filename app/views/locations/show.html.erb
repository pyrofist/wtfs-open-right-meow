<h1><%= @location.formatted_address %></h1>
<%= Cat.get_and_display({gif: false, size: "small"}) %>
<% @location.nearby_restaurants.each do |restaurant| %>
  <p>
    <div>
      <%= restaurant[:name] %>
    </div>
    <div>
      <%= restaurant[:address] %>
    </div>
  </p>
<% end %>
